import { TodayScreen } from '@/features/today/today-screen';

/** Hosts the offline Today experience and first-run onboarding at the app's root route. */
export default function HomeScreen() {
  return <TodayScreen />;
}
